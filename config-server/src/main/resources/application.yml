server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/migdurgon/retoConfigServerMDG.git
        default-label: main
        search-paths:
        - '*service*'
